[project]
name = "python-demo"
version = "0.1.0"
description = "カウンターデモアプリ（Python/Flask版）"
requires-python = ">=3.12"
dependencies = ["flask>=3.1.3"]

[dependency-groups]
dev = ["pytest>=8.4.2"]

[tool.pytest.ini_options]
pythonpath = ["."]
